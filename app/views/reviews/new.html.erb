<h1>Ajouter une review</h1>
<p><%= form_for([@restaurant, @review]) do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.label :rating %>
  <%= f.number_field :rating%>
  <%= f.submit %>
<% end %></p>

